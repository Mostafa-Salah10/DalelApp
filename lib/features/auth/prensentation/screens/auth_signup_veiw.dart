import 'package:flutter/material.dart';

class AuthSignupVeiw extends StatelessWidget {
  const AuthSignupVeiw({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hello signup")),
    );
  }
}
